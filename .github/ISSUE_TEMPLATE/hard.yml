name: Transform the image (${difficulty})
description: A form to select the options available for transforming your current image.

body:
  - type: markdown
    attributes:
      value: |
        ### You can select as many of these as you want:
  - type: checkboxes
    id: multi-selection
    attributes:
      label: Choose multiple
      options:
        - label: works
          required: false
        - label: interesting
          required: false
        - label: this
          required: false
        - label: data
          required: false
        - label: is
          required: false
